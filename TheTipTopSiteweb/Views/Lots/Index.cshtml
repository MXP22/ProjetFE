@model IEnumerable<TheTipTopSiteweb.Models.Lot>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomLot)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescriptionLot)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomLot)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescriptionLot)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idlot">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idlot">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idlot">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
